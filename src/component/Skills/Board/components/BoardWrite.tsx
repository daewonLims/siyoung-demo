import React from "react"

type BoardProps = {
    [key:string]:any;
  };
  
export const BoardWrite: React.FC<BoardProps> = ({  }) => (
<div>
    BoardWrite
</div>
);

// BoardWrite.defaultProps = {
// };

export default BoardWrite;